use 'sake-outdated'
use 'sake-publish'
use 'sake-version'

task 'build', 'Build project', ->
  exec 'coffee -bcm -o lib/ src/'

task 'gh-pages', 'Generate gh-pages', ->
  brief = require './'
  brief.update()
